# Basic requirements for Vercel deployment
# Note: F5-TTS requires significant resources and may not work well on Vercel's serverless platform

# Core dependencies (minimal for basic API)
flask==2.3.3
werkzeug==2.3.7

# For file handling
python-multipart==0.0.6

# Note: For full F5-TTS functionality, you would need:
# torch>=2.0.0
# torchaudio>=2.0.0
# soundfile>=0.12.1
# omegaconf>=2.3.0
# hydra-core>=1.3.0
# cached-path>=1.6.0
# tqdm>=4.65.0
# But these are too heavy for Vercel's serverless environment
