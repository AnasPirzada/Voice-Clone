# Optimized requirements for Render deployment
# Core F5-TTS dependencies
torch>=2.0.0,<2.1.0
torchaudio>=2.0.0,<2.1.0
transformers>=4.30.0
accelerate>=0.20.0
datasets>=2.12.0
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.10.0
numpy>=1.24.0
omegaconf>=2.3.0
hydra-core>=1.3.0
cached-path>=1.6.0
tqdm>=4.65.0

# Flask and web dependencies
Flask>=2.3.0
Flask-CORS>=4.0.0
gunicorn>=21.0.0

# Audio processing
pydub>=0.25.0
pyaudio>=0.2.11

# Hugging Face
huggingface-hub>=0.16.0
safetensors>=0.3.0

# Optional: Remove heavy dependencies for Render free tier
# Uncomment these if you get memory issues:
# --no-deps torch torchaudio
# --no-deps transformers
